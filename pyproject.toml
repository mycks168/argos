[project]
name = "argos"
version = "0.1.0"
description = "ARGOS: Autonomous Road Guardian & Observation System"
# lgpio 0.2.2.0 only publishes wheels through CPython 3.12. Keeping the
# project below 3.13 lets uv select/download a compatible interpreter before
# bootstrap needs to install the source-build toolchain.
requires-python = ">=3.11,<3.13"
dependencies = [
    "adafruit-circuitpython-rgb-display",
    "gpiozero",
    "lgpio",
    "numpy>=1.26",
    "onnxruntime>=1.20",
    "pillow",
    "python-dotenv",
    "requests",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-cov",
]
face = [
    "opencv-python-headless",
]
kokoro = [
    "kokoro>=0.9.4",
    "misaki[ja]>=0.9.4",
    "soundfile>=0.13.1",
]
whisper = [
    "faster-whisper>=1.2.1",
]
wakeword = [
]

[project.scripts]
argos = "argos.main:main"
argos-agent-runner = "argos.agent_runner_main:main"
argos-install = "argos.installer:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
addopts = "--cov=argos --cov-report=term-missing --cov-fail-under=80"
testpaths = ["tests"]

[tool.coverage.run]
omit = [
    "src/argos/hardware/gpio.py",
    "src/argos/main.py",
]
