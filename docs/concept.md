# コンセプト

ARGOS は、運転中や手が離せない場面でも、物理 PTT スイッチだけで Codex CLI に作業を依頼し、応答を音声で受け取れることを目的にする。

中核方針:

- 音声処理の重い推論は GPU サーバ側に寄せる
- Raspberry Pi は録音、GPIO、再生、制御に集中する
- Codex CLI の既存認証と設定を活かす
- Codex が追加確認を返した場合も、次の PTT 入力を同じ会話へ送る
- ダブルクリックで作業対象や会話スロットを切り替える

